{
  "name": "streamsai-workspace",
  "version": "1.0.0",
  "private": true,
  "workspaces": [
    "apps/*"
  ],
  "scripts": {
    "dev:web": "npm run dev --workspace=apps/web",
    "dev:chat": "npm run dev --workspace=apps/mobile-chat",
    "dev": "concurrently \"npm run dev:web\" \"npm run dev:chat\"",
    "build:web": "npm run build --workspace=apps/web",
    "build:chat": "npm run build --workspace=apps/mobile-chat",
    "build": "npm run build:chat && npm run build:web",
    "typecheck": "npm run typecheck --workspace=apps/web && npm run typecheck --workspace=apps/mobile-chat",
    "lint": "npm run lint --workspace=apps/web && npm run lint --workspace=apps/mobile-chat",
    "quality-gate:ci": "node apps/web/scripts/quality-gate-ci.mjs"
  },
  "devDependencies": {
    "concurrently": "^8.2.2"
  }
}